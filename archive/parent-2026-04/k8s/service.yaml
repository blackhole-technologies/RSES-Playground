apiVersion: v1
kind: Service
metadata:
  name: rses-cms
  namespace: rses-cms
  labels:
    app.kubernetes.io/name: rses-cms
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: rses-cms
