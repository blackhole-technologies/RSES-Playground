# Session Handoff — 2026-04-14

**For the next Claude Code session.**

You are continuing work on `/Users/Alchemy/Projects/experiments/RSES-Playground`.
Four hardening passes were completed on 2026-04-14, taking the project
from a ~6.0/10 baseline to ~8.0/10. This file tells you exactly what's
done, what's next, and what NOT to break.

**Read these first, in order:**
1. This file (handoff context)
2. [`docs/STATUS-LATEST.md`](../STATUS-LATEST.md) → currently points to `STATUS_2026-04-14_v4.md`
3. [`docs/ROADMAP-LATEST.md`](../ROADMAP-LATEST.md) → currently points to `ROADMAP_2026-04-14_v4.md`
4. [`docs/security/TRUST-MODEL.md`](../security/TRUST-MODEL.md) — what the kernel does and does not protect
5. [`docs/security/TENANT-ISOLATION.md`](../security/TENANT-ISOLATION.md) — how the `scoped(siteId)` helper works

Do **not** read the older `REVIEW.md` or `PHASE-ALIGNMENT.md` at the repo
root — they are out of date. STATUS-LATEST is canonical.

---

## Current state (snapshot)

| Metric | Value |
|---|---|
| TypeScript errors (`npm run check`) | **0** |
| Security tests (`npm run test:security`) | **351 / 351 passing** |
| Composite score (per STATUS v4) | **~8.0 / 10** |
| Doc versions | v1 → v4, all immutable, dated, in `docs/STATUS_2026-04-14_v*.md` and `docs/ROADMAP_2026-04-14_v*.md` |
| Git status | **Large uncommitted diff** spanning the four passes. The user has not asked for a commit yet. |

**Run these first to confirm the baseline before making any change:**
```bash
npm run check          # must report 0 errors
npm run test:security  # must report 351 passing
```

If either of those fails on a clean checkout, **stop and ask the user**
before doing anything else.

---

## What was accomplished across the four 2026-04-14 passes

### M0 (morning) — honest documentation + 3 critical security fixes
- Wrote `STATUS_2026-04-14_v1.md` and `ROADMAP_2026-04-14_v1.md` (versioned-immutable convention).
- Fixed WebSocket session validation (`server/ws/index.ts`) — now runs the real session middleware on the upgrade request via `noServer: true` + `server.on('upgrade', ...)`.
- Hardened `server/lib/safe-expression.ts` with `MAX_EXPRESSION_LENGTH`, `MAX_PARSE_DEPTH`, `MAX_OPERATIONS` guards.
- Added `server/lib/tenant-scoped.ts` query helper for multi-tenant tables.
- Rewrote `server/kernel/sandbox.ts` to be honest (it's a policy layer, not isolation).
- Documented the file-watcher tier architecture in `docs/architecture/FILE-WATCHERS.md`.
- Repo hygiene: deleted `.bak` files, `gitignore`'d them, deleted `handoff-protocol.txt`, moved `test-editor-load.mjs` to `scripts/`.
- Rewrote `README.md` to match reality.

### M1 (afternoon) — enforcement layer
- Per-module init/start/stop timeout in `server/kernel/registry.ts` (30s `Promise.race`).
- Fail-closed RBAC marker pattern in `server/middleware/rbac-protect.ts` with five wrappers: `protect`, `protectAll`, `protectAny`, `authRoute`, `publicRoute`.
- AsyncLocalStorage tenant scope tracking in `server/lib/tenant-scope-context.ts` (split from tenant-scoped.ts to avoid pulling in `db.ts` for tests).
- Optional Redis-backed rate limiter in `server/kernel/rate-limiter.ts` with `InMemoryRateLimiter` and `RedisRateLimiter` backends and a graceful-fallback factory.

### M1.5 (evening) — first route migration + CI gate
- Migrated `server/routes/admin-audit.ts` to the marker pattern. **This is the template for the rest.**
- Added `tests/security/rbac-marker-coverage.test.ts` lint test that scans `MIGRATED_FILES` for unmarked handlers.
- Wired `.github/workflows/ci.yml` with three jobs:
  - `security-tests` (hard gate, no DB, ~15s)
  - `typecheck` (was soft; promoted to hard gate in v4)
  - `build-and-test` (depends on security-tests)
- **Reverted Anthropic integration** per user instruction. Deleted `server/services/ai/anthropic-client.ts` and reverted `meeting-service.ts` `generateAISummary` to its mock form. **The user does not want to spend on API costs yet.** Do not re-add the SDK without explicit go-ahead.

### M1.6 (late evening) — TypeScript baseline → 0
- Cleaned up 691 → 0 TypeScript errors across two passes (cascade roots in v3, individual fixes in v4).
- The seven cascade root patterns documented in v3:
  1. Enum imports under `import type` (workflow-engine, reactive-streams, saga, etc.)
  2. Duplicate trailing `export {}` blocks (sync/*, infrastructure-adapters, two cms type files)
  3. Express `req.params.X` typed as `string | string[]` → per-call `String()` coercion
  4. Ambiguous re-exports in `shared/messaging/index.ts` → explicit re-exports of just constants
  5. `cqrs-es/index.ts` initializer references → added local imports
  6. `BaseSubscription.onRequest`/`onCancel` `protected` → `public`
  7. Missing `WSMessageType` collaboration entries → extended union
- v4 individual-error pass: ~95 distinct fixes across ~50 files. Patterns:
  - `as unknown as T` for cross-shape casts (Drizzle vs Zod, OAuth2Tokens vs Record, CommandMetadata vs Record)
  - Closure-capture `const eventEmitter = this.eventEmitter` for object-literal `this` issues
  - `String(req.params.X)` coercion at every destructure site
  - Adding missing fields to schemas instead of removing call sites
  - Local imports for re-exported symbols used in same-file class scope

---

## What's NOT done — sequenced for the next session

### Top priority: M1.7 — Migrate the rest of the route files (1 week of work)

`server/routes/admin-audit.ts` is the **template**. Copy that pattern. For each file:

1. Wrap every handler in `protect("permission:key", async (req, res) => {...})` or `authRoute(...)` or `publicRoute(...)`.
2. Add the file path to `MIGRATED_FILES` array in `tests/security/rbac-marker-coverage.test.ts`.
3. Run `npx vitest run tests/security/rbac-marker-coverage.test.ts` — **must pass** before moving on.
4. Run `npm run check` to verify no type regressions.
5. **Do not commit unless the user asks.**

**Priority order** (highest first):

| File | Routes | Why this priority |
|---|---|---|
| `server/routes/admin-rbac.ts` | ~12 | Admin-only, RBAC management — meta-critical |
| `server/routes/sdk-api.ts` | ~5 | Public API surface |
| `server/services/feature-flags/routes.ts` | ~14 | Highest traffic admin path |
| `server/services/feature-flags/site-routes.ts` | ~5 | Site-scoped feature flag operations |
| `server/routes/admin-sites.ts` | ~6 | Admin-only, site management |
| `server/routes/admin-users.ts` | ~6 | Admin-only, user management |
| `server/routes/incidents.ts` | ~7 | Incident management |
| `server/routes/messaging.ts` | ~25 | Largest file; do this LAST or break into chunks |
| `server/routes/automation.ts` | ~5 | Workflow management |
| `server/routes/projects.ts` | ~6 | Project CRUD |
| `server/routes/assistant.ts` | ~3 | Smallest, safe pilot |
| `server/routes/intelligent-watcher-admin.ts` | ~5 | Niche admin tool |

**RBAC permission keys** to use are documented in
`server/services/rbac/rbac-service.ts` and the rbac schema in
`shared/rbac-schema.ts`. Pick the most-specific permission per route. If
no permission exists, use `authRoute()` and add a TODO comment.

### Second priority: M1.7-CI — Coverage on PRs (1 day)

- Add `vitest --coverage` to a new CI job.
- Set 70% line coverage threshold on `server/lib/`, `server/auth/`, `server/middleware/`.
- Surface delta in PR comments via a CI-bot comment action.

### Third priority: M1.4 — Postgres row-level security (1 week + DBA review)

This was deferred across all four passes because it needs real DB
migration testing. Notes for whoever picks it up:

- Top 3 multi-tenant tables: `siteFeatureOverrides`, `featureRolloutHistory`, plus one new tagged table.
- Schemas in `shared/schema.ts` already have `siteId` columns.
- Policies need to enforce `siteId = current_setting('app.current_site_id')` or similar.
- Application layer must set the session variable per-request via the existing tenant isolation middleware in `server/middleware/tenant-isolation.ts`.
- Bypass-attempt tests must be added to `tests/security/`.
- Rollout via a single migration in `migrations/`, gated behind a feature flag for safety.

### Held: M2 — Anthropic integration (DO NOT DO without user opt-in)

The user explicitly deferred this in v3 because they don't want to incur
API costs yet. **Do not re-add `@anthropic-ai/sdk` to `package.json`.**
**Do not re-create `server/services/ai/anthropic-client.ts`.**
**Do not re-wire `meeting-service.ts` `generateAISummary` to call Claude.**

When the user is ready, the work to revive it is:
1. Restore the wrapper file from git history at the M0 commit (search the diff for `anthropic-client.ts`).
2. Re-wire the meeting-service path with the try-Anthropic-then-fallback pattern.
3. Add `@anthropic-ai/sdk` to package.json and run `npm install`.
4. Set `ANTHROPIC_API_KEY` in the dev environment.
5. Add per-site token budgets, persist meeting transcripts, build admin UI tab, write integration tests.

---

## Hard rules — do NOT break these

1. **Never commit anything without explicit user instruction.** Global rule. The four-pass diff is large and uncommitted; the user reviews and commits manually.
2. **CI typecheck job is a hard gate.** `.github/workflows/ci.yml` `typecheck` job has NO `continue-on-error`. If you introduce a type error, fix it before claiming done.
3. **CI security-tests job is a hard gate.** Same logic — the 351 tests must remain green.
4. **No new `Math.random()` in business logic.** It was the most common code smell in the original audit. Use only for jitter/IDs.
5. **No `@anthropic-ai/sdk` install.** See above.
6. **No new files at the repo root.** Use `docs/`, `server/`, `client/`, `scripts/`, etc. Repo root has been cleaned.
7. **STATUS and ROADMAP are versioned-immutable.** New revisions get a new file `STATUS_YYYY-MM-DD_vN.md` (or ROADMAP). Update the LATEST pointers; never edit a published version in place.
8. **The kernel sandbox is policy, not isolation.** Don't add code that loads user-supplied modules. The install endpoint at `server/kernel-integration.ts:912` is intentionally disabled.
9. **Multi-tenant table queries go through `scoped(siteId)`.** The lint test in `tests/security/tenant-scoped-imports.test.ts` enforces it. If you add a new tagged table, register it in `server/lib/tenant-scoped-registry.ts`.
10. **Use the marker pattern for new routes.** `protect/protectAll/protectAny/authRoute/publicRoute` from `server/middleware/rbac-protect.ts`. Add the file to `MIGRATED_FILES` so the lint enforces coverage.

---

## Useful file locations cheat sheet

| What | Where |
|---|---|
| Latest authoritative status | `docs/STATUS-LATEST.md` → `docs/STATUS_2026-04-14_v4.md` |
| Latest roadmap | `docs/ROADMAP-LATEST.md` → `docs/ROADMAP_2026-04-14_v4.md` |
| Trust model (security boundaries) | `docs/security/TRUST-MODEL.md` |
| Tenant isolation guide | `docs/security/TENANT-ISOLATION.md` |
| File-watcher architecture | `docs/architecture/FILE-WATCHERS.md` |
| Master plan (24-month vision) | `docs/plans/CMS-MASTER-PLAN-FINAL.md` |
| RBAC marker wrappers | `server/middleware/rbac-protect.ts` |
| Marker coverage lint test | `tests/security/rbac-marker-coverage.test.ts` |
| Tenant-scoped query helper | `server/lib/tenant-scoped.ts` |
| Tenant scope context | `server/lib/tenant-scope-context.ts` |
| Tenant table registry | `server/lib/tenant-scoped-registry.ts` |
| Tenant import lint test | `tests/security/tenant-scoped-imports.test.ts` |
| Express helpers | `server/lib/express-helpers.ts` |
| Safe expression evaluator | `server/lib/safe-expression.ts` |
| Safe expression tests | `tests/security/safe-expression.test.ts` |
| Module timeout helper + tests | `server/kernel/registry.ts` + `tests/security/module-timeout.test.ts` |
| Rate limiter (in-memory + Redis) | `server/kernel/rate-limiter.ts` + `tests/security/rate-limiter.test.ts` |
| WebSocket session validation | `server/ws/index.ts` (uses `getSessionMiddleware()` from `server/auth/session.ts`) |
| Migrated route example | `server/routes/admin-audit.ts` (template for M1.7) |
| Express type augmentations note | `server/types/express-augmentations.d.ts` (placeholder doc — augmentation didn't work) |
| CI workflow | `.github/workflows/ci.yml` |

---

## When to ask the user vs. proceed autonomously

**Proceed autonomously:**
- Migrating route files to the marker pattern (M1.7).
- Adding tests for newly migrated files.
- Fixing any new TypeScript errors you accidentally introduce.
- Updating `MIGRATED_FILES` in the lint test as you migrate files.

**Ask first:**
- Anything that touches `package.json` dependencies.
- Postgres RLS migrations (M1.4).
- Adding a new external service integration.
- Committing or pushing.
- Deleting any file that isn't generated/temporary.
- Any change that would lower the security test count or introduce a typecheck error.

---

## What's in git status (uncommitted diff summary)

The four 2026-04-14 passes touched ~80 files. The diff is too large to
list here but is cohesive: every change is documented in the v1→v4
STATUS chain. Run `git status --short` to see the current picture, and
`git diff <file>` to review individual changes before committing.

The diff includes:
- ~12 new files in `server/lib/`, `server/middleware/`, `server/types/`, `tests/security/`, `docs/`, `docs/handoffs/`, `docs/architecture/`, `docs/security/`.
- Modifications to ~60 existing files across `server/`, `client/`, `shared/`, and `.github/`.
- Two file renames: `client/src/design-system/hooks/useDesignSystem.ts` → `.tsx`, `client/src/hooks/use-accessibility.ts` → `.tsx`.
- Two deletions: `server/services/ai/anthropic-client.ts` and `handoff-protocol.txt`.
- One file move: `test-editor-load.mjs` → `scripts/test-editor-load.mjs`.

---

## Trajectory chart

```
session start (audit only)           6.0
v1 morning (M0)                      6.0 → 6.5  honest docs + 3 critical security fixes
v2 afternoon (M1)                    6.5 → 7.0  enforcement (timeout, RBAC markers, scope, rate limiter)
v3 evening (cleanup + M1.5)          7.0 → 7.5  691 → 175 errors, first migration, CI gate
v4 late evening (TS baseline → 0)    7.5 → 8.0  zero errors, hard typecheck gate
↓
next session (M1.7)                  8.0 → ~8.3  finish route migrations
+1 session (M1.4 RLS)                ~8.3 → ~8.6  Postgres-layer tenant isolation
+1 session (M1.7-CI coverage)        ~8.6 → ~8.8  coverage visible on PRs
```

---

*This handoff is immutable. Future handoffs get a new file
`docs/handoffs/HANDOFF_YYYY-MM-DD_vN.md`. Update STATUS in the same PR
that drains the work tracked here.*
