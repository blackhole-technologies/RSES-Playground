# RSES-Playground — Roadmap

**Version:** 2
**Last updated:** 2026-04-14 (afternoon — after M1 hardening pass)
**Supersedes:** `ROADMAP_2026-04-14_v1.md`
**Companion doc:** `docs/STATUS_2026-04-14_v2.md`
**Vision doc:** `docs/plans/CMS-MASTER-PLAN-FINAL.md` (24-month aspiration)

> v2 records the M0 → M1 progress, marks shipped items, and re-sequences the
> remaining work. The principles are unchanged from v1: honesty before scope,
> enforcement before expansion, one real AI path before fanning out, delete
> before adding, tests are part of the deliverable.

---

## Milestone 0 — 2026-04-14 morning ✅ SHIPPED

Documented in v1. All items complete. Core wins:
- Authoritative STATUS + ROADMAP, immutable + dated.
- WebSocket session validation, safe-expression hardening, tenant query helper.
- Sandbox honesty pass (rewritten as policy/audit, not isolation).
- Anthropic client wrapper (graceful no-op fallback) wired into meeting summaries.
- Repo hygiene + README rewrite.
- 309/309 security tests passing.

---

## Milestone 1 — 2026-04-14 afternoon ✅ MOSTLY SHIPPED

| # | Item | Status | Files |
|---|---|---|---|
| 1.1 | Per-module init/start/stop timeout in kernel registry | ✅ Shipped | `server/kernel/registry.ts`, `tests/security/module-timeout.test.ts` |
| 1.2 | Redis-backed rate limiter for kernel gateway | ✅ Shipped | `server/kernel/rate-limiter.ts`, `server/kernel/gateway.ts`, `tests/security/rate-limiter.test.ts` |
| 1.3 | Drizzle scope guard + structural lint test | ✅ Shipped (lint test path) | `server/lib/tenant-scope-context.ts`, `server/lib/tenant-scoped.ts`, `tests/security/tenant-scoped-imports.test.ts` |
| 1.4 | Postgres row-level security policies | ⏳ Deferred — needs DBA review and migration testing | n/a |
| 1.5 | Fail-closed RBAC handler markers | ✅ Shipped | `server/middleware/rbac-protect.ts`, `tests/security/rbac-fail-closed.test.ts` |
| 1.6 | Wire `npm run test:security` into CI gate | ⏳ Not started | n/a |
| 1.7 | Coverage on PRs | ⏳ Not started | n/a |
| 1.8 | TRUST-MODEL.md | ✅ Shipped in M0 | `docs/security/TRUST-MODEL.md` |

**Test count:** 309 → 347 security tests passing (+38 across 4 new files).

**M1 outcome:** Composite score 6.5 → 7.0. The dueling self-grades are gone
(M0), the three critical security gaps are closed at the most exploitable
layers (M0), AND the foundation now has fail-closed enforcement at the
handler layer (M1.5), the module lifecycle layer (M1.1), the tenant scope
layer (M1.3), and the rate limiter layer (M1.2).

---

## Milestone 1.5 — Finish what M1 deferred (1 week)

Three small follow-ups before moving on to the AI service work:

| # | Item | Owner | Acceptance |
|---|---|---|---|
| 1.5.1 | M1.4: Postgres RLS policies for the top 3 multi-tenant tables | Data + DBA | RLS policies on `siteFeatureOverrides`, `featureRolloutHistory`, and one new tagged table; bypass-attempt tests in `tests/security/`; rolled out via a single migration |
| 1.5.2 | M1.6: Wire `npm run test:security` into pre-commit + CI | DevEx | A failing security test blocks both local commit and CI merge |
| 1.5.3 | M1.7: Coverage report in CI artifacts | DevEx | Coverage HTML on every PR, threshold 70% on `server/lib/`, `server/auth/`, `server/middleware/` |
| 1.5.4 | Migrate at least one route file to `protect()`/`authRoute()`/`publicRoute()` markers as a proof | Security | One real route file in `server/routes/` migrated end-to-end with no behavior changes |
| 1.5.5 | Add a CI-time scan for unmarked handlers in routes that have been migrated | DevEx | A second lint test (companion to tenant-scoped-imports) that fails if any handler in migrated files is unmarked |

**Exit criteria:** Phase 1 is structurally complete. Tenant isolation has
all three layers (request, query, DB). RBAC has fail-closed enforcement
at the handler layer with at least one migrated route file. CI gates the
security suite. Coverage is visible.

---

## Milestone 2 — One honest Phase-2 service (3 weeks)

Unchanged from v1: meeting summaries via Anthropic. The wrapper exists in
M0 (`server/services/ai/anthropic-client.ts`); meeting-service.ts already
calls it with graceful fallback. M2 is the **product** layer:

| # | Item | Acceptance |
|---|---|---|
| 2.1 | Add `@anthropic-ai/sdk` to package.json | One-line dep, on user's go-ahead |
| 2.2 | Verify the M0-wired path end-to-end with a real key in a dev env | Run a real meeting through the path, check the model field, confirm the JSON parse contract |
| 2.3 | Per-site token budget enforcement, surfaced in admin UI | Per-site daily token cap; over-budget calls return a graceful error |
| 2.4 | Persist meeting transcripts to a real `meetings` table (drizzle migration) | New tables with FKs to `users` and `sites` |
| 2.5 | Frontend: meetings tab in admin UI listing recent summaries | Real React page using React Query, accessibility parity with feature-flags page |
| 2.6 | Integration test mocking Anthropic, covering happy path + budget exhaustion + outage | `npm run test` covers the full flow |

---

## Milestone 3 — Replicate the pattern (4 weeks)

Unchanged from v1.

| # | Service | Real Backend | Today | Goal |
|---|---|---|---|---|
| 3.1 | `ai-content-service.ts` | Anthropic + cached prompts | `Math.floor(Math.random() * 30) + 70` | Real Claude rubric scoring |
| 3.2 | `ml-taxonomy-engine.ts` | Embeddings + cosine similarity | `0.85 + Math.random() * 0.1` | Real OpenAI embeddings via `embedding-providers.ts` |
| 3.3 | `auto-taxonomy-learner.ts` | Online learning loop | Random updates | Held-out evaluation set |

---

## Milestones 4–6 — Phase 2/3/4 integration (unchanged from v1)

See v1 for full descriptions. Summary:
- **M4** Phase 2 integration: WebSocket channels, calendar OAuth (real Google), trigger system with HMAC.
- **M5** Phase 3 selective: mount existing sync code, build site-network admin page. Defer social platform connectors and analytics.
- **M6** Phase 4 (open): WebRTC only on stakeholder ask. Rename "AIOps" to "Operational Analytics" until there is real ML.

---

## Explicit non-goals (unchanged from v1)

- ❌ Quantum-ready features.
- ❌ Hosting untrusted third-party plugins.
- ❌ Building our own LLM training pipeline.
- ❌ A new editor.
- ❌ GraphQL gateway.
- ❌ Microservices split.

---

## Risk register update

| Risk | v1 status | v2 status | Δ |
|---|---|---|---|
| Drift between docs and code returns | Active | **Mitigated by M0 pattern** | STATUS/ROADMAP versioning + LATEST pointers are working |
| Phase 2 services accumulate more Math.random scaffolds | High | High | Hold the line — M1.5 finishes first |
| Anthropic spend grows unbounded | Medium | Medium | Wrapper exists; budget enforcement is M2.3 |
| Multi-tenant data leak from missed WHERE clause | Medium | **Lower** | M1.3 lint test catches the import pattern at PR time. RLS (M1.5.1) is the next step. |
| Kernel sandbox mistaken for isolation | Low | **Lower** | M0 file rewrite + TRUST-MODEL.md + runtime warning on first construction |
| README oversells | Medium | Mitigated | Done in M0 |
| Hung module blocks bootstrap | Medium | **Mitigated** | M1.1 timeout shipped |
| Rate limiter desync across replicas | Medium | **Mitigated** | M1.2 Redis backend shipped (opt-in) |
| Routes accidentally ship without RBAC | Medium | **Lower** | M1.5 marker pattern shipped; full migration is M1.5.4 |

---

## How v2 supersedes v1

Both files exist on disk forever. `ROADMAP-LATEST.md` points to v2. Future
revisions get a v3 file. Don't edit v1 or v2 in place after commit. The
diff between v1 and v2 IS the changelog.

---

*Sequenced by leverage. Honest about deferrals. Updated in lockstep with STATUS.*
