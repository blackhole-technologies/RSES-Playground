# HANDOFF — 2026-04-22 v1

For the next session. Current session ended at 79% token usage.

---

## Ground truth: what exists on disk

### 1. The parent repo (`/Users/Alchemy/Projects/experiments/RSES-Playground/`)

Do not work in this. It's the sprawl. 100+ doc files under `docs/`, ~388 source files under `server/`, a multi-tenant RLS migration chain in a half-broken state, three competing master plans, seven roadmap versions, eleven STATUS files. The previous occupant (me, across earlier sessions) produced all of it and then acknowledged the pattern.

The only things worth consulting in the parent repo:
- `server/lib/*.ts` — source of the salvage.
- `tests/security/`, `tests/engine/`, `tests/production/`, `tests/prompting/`, `tests/feature-flags/`, `tests/rls/` — source of the salvaged tests.

Ignore the rest. Do not add new files to `docs/`. Do not produce new STATUS/ROADMAP files in the parent repo's pattern.

### 2. `/salvage/` — 13 self-contained units, 460 passing tests

The real work salvaged from the parent repo. Each subdirectory stands alone: its own README, its own source, its own tests, zero imports from outside itself (except unit 11 which declares a peer dependency on unit 10).

```
salvage/
├── README.md
├── 01-rses-engine/              155 tests
├── 02-feature-flags/             80 tests
├── 03-logger/                     0 tests
├── 04-security-middleware/       36 tests
├── 05-metrics/                   13 tests
├── 06-circuit-breaker/           17 tests
├── 07-suggestion-engine/         27 tests
├── 08-queue/                     20 tests
├── 09-safe-expression/           13 tests
├── 10-taxonomy-algebra/          40 tests
├── 11-taxonomy-query-engine/     36 tests (imports unit 10)
├── 12-tenant-scope-context/       7 tests
└── 13-password-hash/             16 tests
```

Every unit runs `npx vitest run tests/` from its own directory and passes. The `/salvage/README.md` documents the exceptions where files deviate from verbatim copy.

Rules re: salvage:
- **Do not edit salvage files directly.**
- **Do not add more salvage units.** This is the complete honest list.

### 3. `/cms-spec/` — design documents for an MVP not yet built

```
cms-spec/
├── SPEC.md              ~900 lines — requirements, data model, invariants, non-goals, API catalog
├── DESIGN.md           ~1300 lines — architecture, engines, modules, admin UI, reader UI, public UI
├── IMPLEMENTATION.md    ~700 lines — 9-phase build order, testing, deployment, ops checklist
├── THREAT-MODEL.md      ~600 lines — 31 attack vectors, mitigations, verification matrix
├── PERFORMANCE.md       ~500 lines — per-endpoint budgets, memory/DB budgets, degradation, profiling
├── VISUAL-DIRECTION.md  ~600 lines — design tokens, component baselines, a11y, explicit non-specs
├── templates/
│   └── login.html       ~200 lines — self-contained login page, inline CSS, respects system theme
└── HANDOFF_2026-04-22_v1.md   this file
```

These documents are internally consistent. They describe a multi-user RSS-aggregating CMS with defined extension seams for future features (media, WYSIWYG, layout, Fields API for user-defined content types, modules/engines/plugins).

### 4. What's NOT in this repo (explicitly rejected)

Do not resurrect:

- The multi-tenant RLS initiative from the parent repo.
- Any of the "neural classifier," "vector database," "embedding providers," "ML taxonomy engine," or "quantum" code.
- The `shared/admin/schema.ts` orphan Drizzle declarations.
- The `shared/multisite-schema.ts` file.

---

## Important context: how the user wants to work

Invariants established through several course corrections this session.

### Tone and artifact discipline

1. **No composite scores.** Called out as pretend-rigor.
2. **No "APPROVED FOR EXECUTION" or "AUTHORITATIVE" framing.**
3. **No expert-persona sign-off tables.** No `@author SGT (Set-Graph Theorist Agent)`, no "consulted 9 experts," no agent checkbox lists. Theatre.
4. **No 10-year design horizons for a single-developer project.**
5. **No immutable versioned STATUS / ROADMAP ledgers.** The parent repo has eleven status files and seven roadmaps; every new one made it worse.
6. **Handoffs ARE date-versioned.** Pattern: `HANDOFF_YYYY-MM-DD_vN.md`. One per session boundary. This is a real convention, not the sprawl pattern. I got this wrong once; corrected.
7. **Plain prose, tables where tabular, code samples where specific.** No `★ Insight ─────` boxes.
8. **Where something is uncertain, say so in the doc itself.** Not in a footnote.

### Work discipline

9. **Verify before claiming.** Don't report test counts or anything else without running. I was caught guessing "29 tests" for a salvage unit; actual was 20.
10. **Stop when the premise is wrong.** Three times this session I was mid-execution, found the plan had a substrate problem, and stopped. That's correct.
11. **Don't ask questions while simultaneously creating the artifact with those questions baked in.**
12. **One move at a time** when appropriate. When the user said "one at a time" for the salvage, they meant literally one, with a check-in.
13. **Don't re-derive context from memory.** Read the file.

### Global rules from the user's `~/.claude/CLAUDE.md` (still apply)

- NEVER create files without explicit permission.
- Always ask before creating new directories.
- When creating files, explain WHERE and WHY before doing it.
- Prioritise accuracy over approval.
- Iterate 10 times over decisions before making code changes.
- Only write code when explicitly directed.

---

## Recommended next-session goal

**Start building the MVP per `cms-spec/IMPLEMENTATION.md` Phase 0.**

Phase 0 is ~½ day of work: scaffold Express, wire the logger/metrics/security middleware engines (which wrap salvage units 03, 04, 05), implement core `events.ts` and `hooks.ts`, stand up `config.ts` with env validation. Output: a running HTTP process that responds to `/health` and `/metrics`.

Pre-reading order:
1. `cms-spec/IMPLEMENTATION.md` §1–§3.0 — prerequisites, directory structure, Phase 0 description.
2. `cms-spec/DESIGN.md` §1, §2.1–§2.8 — architecture overview and core services.
3. `salvage/03-logger/README.md`, `salvage/04-security-middleware/README.md`, `salvage/05-metrics/README.md` — the three salvage units Phase 0 wires up.
4. `cms-spec/SPEC.md` §5.1 — bootstrap flow (relevant in Phase 1, but good to read now).

Before writing code, confirm with the user:
1. **Build location.** `app/` was assumed in the spec docs but not explicitly confirmed. Ask before creating.
2. **Package manager.** `pnpm` was assumed; user may prefer `npm`.
3. **Whether to symlink or copy salvage into the build directory.**

First concrete steps once those are confirmed:
1. Create build directory.
2. `mkdir -p` per IMPLEMENTATION.md §2.
3. `pnpm init` (or `npm init -y`).
4. Install deps: `express`, `zod`, `pino`, `pino-pretty`, `helmet`, `express-rate-limit`, `prom-client`, `pg`, `drizzle-orm`, `drizzle-kit`, `tsx`, `typescript`, `vitest`.
5. Get salvage units 03, 04, 05 into the build directory.
6. Write `core/config.ts`, `core/db.ts`, `core/events.ts`, `core/hooks.ts`, `core/bootstrap.ts`.
7. Write `server.ts` entry point.
8. Wire engines 03/04/05 as described in IMPLEMENTATION.md Phase 0.
9. `pnpm dev` should start the server; `curl localhost:3000/health` returns 200.

Don't skip ahead to Phase 1. Phase 0 has a clear done criterion; ship that and check in with the user.

---

## Things that will be easy to mess up

1. **Don't reproduce the genre.** When the user asks for "a design doc" or "a plan," the first instinct is the ceremonial shape. Resist.
2. **Don't guess numbers.** Run the thing; report the real output.
3. **Don't invent agent personas.**
4. **Don't sprawl the docs.** Planning in `cms-spec/`. Salvage in `salvage/`. New build somewhere confirmed with user. Do not add to `docs/`.
5. **Don't "refactor" salvage units.** They're self-contained by design.
6. **Don't recreate the parent repo's multi-tenant RLS initiative.**
7. **Don't silently change scope.** Future features (media, WYSIWYG, layout, Fields API, modules/plugins, comments, search, email) are explicitly later.
8. **Don't write password resets via email.** Admin-initiated only per SPEC §5.3.
9. **Don't build a profile page.** Public surface is only outgoing RSS + individual public posts.
10. **Bootstrap-token flow is security-relevant.** SPEC §5.1. `/setup?token=<wrong>` returns 404, not 401.

---

## Open questions that will resurface

Deferred this session; will come back.

1. Build location (`app/` was assumed, not confirmed).
2. Tooling: pnpm vs. npm.
3. DB host: local Postgres vs. Docker vs. managed.
4. Session secret management (env is fine; verify).
5. Email ever? (Ruled out for MVP. Confirm if user wants to change.)
6. SQLite support? (Said no in deep dive; confirm.)

---

## State of the session (brief)

- Audited the parent repo. Confirmed it's the mess the user described.
- User acknowledged the patterns they wanted stopped: composite scores, agent personas, ceremonial framing, doc sprawl. I acknowledged creating those patterns.
- Salvaged 13 units across 13 separate subdirectories, each with a README and passing tests (460 total).
- Designed a multi-user RSS-aggregating CMS using only what the salvage provides + minimal net-new work.
- Wrote 7 planning documents in `cms-spec/` covering requirements, architecture, implementation, threat model, performance budget, visual direction, login template, and this handoff.
- Did not write any CMS code. The MVP is specified but not built.

---

## Final note

The user has been burned by elaborate specs that aren't built and by code that's claimed to work but doesn't. The value of what's in `salvage/` and `cms-spec/` is that **both are verifiable**: the salvage passes its tests; the specs have no contradictions I can find. Don't compromise either property.

If you must pick between "ship a documented plan" and "ship a working slice," pick the slice. The spec is already written; the next session's job is to turn the first chunk into code.
